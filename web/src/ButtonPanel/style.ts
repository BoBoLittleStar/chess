import styled from "@emotion/styled";

export const Div = styled.div`
  width: max-content;

  text-align: left;
  font-size: 14pt;
  margin-left: 1em;
  margin-right: 1em;
  .content {
    position: absolute;
    button {
      font-size: 14pt;
      margin: 1em;
      padding: 0 1em;
      background: cornsilk;
      border: 1px solid black;
      border-radius: 1em;
    }
  }
`;
